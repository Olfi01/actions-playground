name: Test matrix

on:
  workflow_dispatch:
    inputs:
      one:
        type: boolean
        default: true
      two:
        type: boolean
        default: true

jobs:
  matrix-job:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        label: ${{ toJSON(inputs.*) }}
    steps:
      - run: echo ${{ matrix.label }}
